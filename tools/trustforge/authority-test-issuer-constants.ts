/** Shared TEST issuer constants (no secrets, no signing). */

export const B63_TEST_ISSUER = "trustforge_b63_test_issuer" as const;
export const B63_TEST_ISSUER_KEY_ID = "b63_test_hmac_v1" as const;
